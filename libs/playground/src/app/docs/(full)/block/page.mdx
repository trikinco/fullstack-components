import Prose from '@/src/components/Prose'
import Blocks from './Blocks'

export const metadata = {
  title: 'Block - Generative UI',
}

<Prose className="mb-6">
# Block

Blocks are individual pieces of UI that are generated and mounted on the fly, and all you need is a `prompt`.

```jsx
import { Block } from '@trikinco/fullstack-components/client'
```

```jsx
<Block prompt="A vibrant page section with the text 'Welcome 👋'" />
```

</Prose>

<Blocks />

<Prose className="mt-6">

## Blocks

The UI above is made out of the following blocks:

```jsx
<Block
  prompt={`A navbar with a modern SVG logo, ${NAME_LIB} and some links. Dark bg, light text.`}
  fallback={
    <Spinner
      className="flex gap-3 p-3 items-center"
      classNameSpinner="mb-0"
    >
      Creating navbar...
    </Spinner>
  }
/>
```

```jsx
<Block
  prompt={`A sleek UI hero section with some text about ${NAME_LONG}. Dark bg, light text.`}
  fallback={
    <Spinner
      className="flex gap-3 p-3 items-center"
      classNameSpinner="mb-0"
    >
      Creating hero section...
    </Spinner>
  }
/>
```

```jsx
<Block
  prompt={`A two-column call to action section with a purple button and some text listing some features of ${NAME_LONG} in one column, and a photo from Unsplash in the other column. Dark bg, light text.`}
  fallback={
    <Spinner
      className="flex gap-3 p-3 items-center"
      classNameSpinner="mb-0"
    >
      Creating call to action section...
    </Spinner>
  }
/>
```

```jsx
<Block
  prompt={`A footer with copyright for this year - ${NAME_SHORT}. Dark bg, light text.`}
  fallback={
    <Spinner
      className="flex gap-3 p-3 items-center"
      classNameSpinner="mb-0"
    >
      Creating footer...
    </Spinner>
  }
/>
```

## Setup

1. Add the `handleBlockRequest` to the [API handler route](/docs/get-started/#setup).

```ts title="app/api/fsc/route.ts"
import {
  handleFSComponents,
  handleBlockRequest,
  type FSCOptions,
} from '@trikinco/fullstack-components'

const fscOptions: FSCOptions = {
  handleBlock: handleBlockRequest({
    openAiApiKey: process.env.OPENAI_API_KEY || '',
  }),
  // Additional options and handlers...
}

const fscHandler = handleFSComponents(fscOptions)

export { fscHandler as GET, fscHandler as POST }
```

2. Import and use the `Block` component from `/client`.

```jsx
import { Block } from '@trikinco/fullstack-components/client'
```

</Prose>
